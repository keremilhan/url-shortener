export const baseUrl = import.meta.env.VITE_BASE_URL;

export const loginEndPoint = `${baseUrl}/auth/login`;

export const signUpEndPoint = `${baseUrl}/auth/register`;

export const createUrlEndPoint = `${baseUrl}`;

export const allUrlsEndPoint = `${baseUrl}/get`;

export const deleteUrlEndPoint = `${baseUrl}`;

export const updateUrlEndPoint = `${baseUrl}`;
